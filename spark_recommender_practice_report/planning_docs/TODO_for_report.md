# TODO-лист для финализации отчёта

Файл-чеклист того, что в текущей черновой версии отчёта оставлено как заглушка или допущение, и что нужно дополнить по мере выполнения работ. Чёрный список — то, что точно надо подменить реальными данными до сдачи.

---

## Глава 1 (проектирование)

- [x] Подставить актуальный вариант UML-диаграмм: исходник `../../docs/diagrams/spark_recommender_diagrams.drawio`, SVG-экспорт `../../docs/diagrams/*.svg`.
- [ ] Если научный руководитель попросит — добавить отдельную диаграмму состояний (state machine) для жизненного цикла модели (trained → registered → deployed → deprecated).
- [x] Проверить, что финальные имена эндпоинтов совпадают с реализацией. Финальный список: `/recommend`, `/predict`, `/jobs`, `/metrics`, `/health`, `/history`, `/history/stats`, `/admin/models`, `/admin/models/reload`. Имена не изменятся.

## Глава 2 (расширение датасета)

- [ ] Подставить реальные размеры собранных датасетов. Уже готово: PageRank `data/hibench_train_20260424_175032_clean.csv` — 917 строк после очистки из 960; WordCount baseline `data/wc_train_merged.csv` — 401 строка. TeraSort/WordCount v2/KMeans ещё ждут сбора.
- [ ] Уточнить **фактическое** время сбора каждой нагрузки. План: TeraSort ≈ 2 сут, PageRank ≈ 4.7 сут, WordCount ≈ 1.5 сут. Замерить и подставить.
- [ ] Добавить таблицу со статистикой собранного датасета: распределение `median_duration_s` по job_type × profile, доля строк с `cv_duration > 0.25` (кандидаты на отбраковку), распределение по топологиям.
- [ ] Поправить даты на скриншотах мониторинга и обзора VM, если они окажутся в отчёте слишком уж старыми к моменту сдачи (сейчас 24-25 апреля 2026).
- [ ] Уточнить у Платонова Алексея Владимировича, ОК ли упоминать прямую цитату из Sensors22 («These four applications represent different workloads of Apache Spark...») в обоснование выбора нагрузок — если ОК, оставить, если нет — заменить на пересказ.

## Глава 3 (модели прогнозирования)

- [ ] После сбора нового мульти-нагрузочного датасета — переобучить baseline RF+SA, переобучить DNN, посчитать новые метрики (MAE, RMSE, R², MAPE) для **каждой** нагрузки отдельно. Уже есть PageRank baseline: RandomForest_RandomSearch, MAE≈8.97s, R²≈0.844; WordCount baseline: RF+SA, MAE≈0.746s, R²≈0.961.
- [ ] Запустить multi-task learning эксперимент: одна модель на всех нагрузках с признаком `job_type` через one-hot. Сравнить с per-workload моделями.
- [ ] Реальные числа speedup для RL-методов на новом датасете. Сейчас в отчёте плановые: ожидаем ~1.15-1.25× с учётом улучшенного датасета.
- [ ] Графики: «MAE по job_type для всех моделей», «Predicted vs Actual scatter для лучшей модели на каждой нагрузке». Делать через `scripts/plot_combined.py`.

## Глава 4 (REST API)

- [x] Реальный Dockerfile, docker-compose.yml для сервиса, requirements-prod.txt → листинг в Приложении.
- [ ] Скриншот OpenAPI `/docs` страницы с реально работающим сервисом.
- [ ] Пример ответа на `POST /recommend` с реальными числами (не плановыми) — добавить как Listing.
- [x] Заменить заглушку «вынесено в отдельный файл api_and_testing_plan.md» на реальную секцию по реализации.

## Глава 5 (тестирование)

- [x] Реальный отчёт `pytest --cov` (79 тестов, покрытие ≈93%).
- [x] Скрипт E2E-валидации `scripts/e2e_validate.sh` написан: запускает реальный HiBench PageRank с рекомендованным конфигом и выводит MAPE predicted vs actual. Запустить: `WORKERS=4 CORES=2 RAM_GB=4 bash scripts/e2e_validate.sh`.
- [ ] Реальная таблица «predicted vs actual» (нужно запустить e2e_validate.sh и вставить числа в отчёт).
- [ ] Реальный отчёт `locust` с p50/p95.
- [ ] Скриншот Spark UI с прогоном задачи по рекомендованной конфигурации (для Приложения).

## Глава 6 (документация)

- [ ] Финальная ссылка на репозиторий (правильный коммит/тег к моменту защиты).
- [ ] README в репозитории должен быть согласован с отчётом — секции, имена скриптов, диаграммы.
- [ ] Если успеется — записать короткое демо-видео и приложить ссылку (опционально).

## Заключение

- [ ] Подставить реальные итоговые числа: лучший MAE, лучший speedup, число строк датасета, число успешных integration-тестов, p50/p95 API.

---

## Открытые вопросы / надо уточнить с руководителем

1. Нужен ли в отчёте отдельный раздел про MLflow Model Registry, или достаточно «версионирование через файловую папку + `model_metadata.json`»?
2. Какая глубина описания требуется для UML-диаграмм — достаточно ли use case + components + sequence + class, или нужны ещё activity и deployment?
3. Включаем ли мы KMeans в датасет или оставляем как «опционально, если позволит бюджет VM-времени»? В текущей черновой версии описано как опциональное.
4. По C++ для сервиса: прорабатывать как реальную ветку (готовить ONNX-экспорт RF, делать `lleaves`-инференс) или ограничиться обоснованием почему остаёмся на Python? В черновике сейчас второй вариант. Если есть желание сделать первый — это +неделя работы.
5. Нужна ли отдельная глава про **этические аспекты использования AI в принятии решений по Spark-конфигурациям** — обычно для магистерской ВКР такое спрашивают, но для производственной практики не обязательно. Уточнить.

---

## Что лежит в этом архиве

- `report/НИР_Апыхин_Артем_P4235_practice_2.docx` — черновая версия отчёта.
- `report/monitoring_screenshot.png`, `report/vm_overview_screenshot.png` — скриншоты Yandex Cloud, вставлены в Главу 2 отчёта.
- Shell-скрипты с русскими шапками теперь живут только в `../../scripts/`; папка `attachments_scripts/` удалена как дубль.
- `../../docs/diagrams/spark_recommender_diagrams.drawio` — исходник UML-диаграмм (5 листов: use cases, components, classes, sequence, data flow). SVG-экспорт лежит рядом в `../../docs/diagrams/*.svg`.
- `planning_docs/api_and_testing_plan.md` — детальный план реализации REST API и тестов, под которым в отчёте сейчас стоят заглушки (главы 4 и 5).
- `planning_docs/TODO_for_report.md` — этот файл.
